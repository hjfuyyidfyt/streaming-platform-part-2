/**
 * AdBanner - Monetag Multitag handles all banner ads automatically.
 * This component is kept as a no-op placeholder for compatibility.
 */
const AdBanner = ({ format = "banner", placement = "banner_home", className = "" }) => {
    // Monetag multitag script in index.html handles banner ads automatically
    return null;
};

export default AdBanner;
